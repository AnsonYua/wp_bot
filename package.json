{
  "name": "weather-ev-monitor",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check api/check.js && node --check api/rules.js && node --check api/cache.js",
    "dev": "vercel dev"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@polymarket/clob-client-v2": "^1.0.6",
    "@vercel/blob": "^0.27.3",
    "ethers": "^6.16.0"
  },
  "devDependencies": {
    "vercel": "^34.0.0"
  }
}
